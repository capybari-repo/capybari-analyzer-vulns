name: ci
on: [push, pull_request]
jobs:
  ci:
    uses: capybari-repo/capybari-core/.github/workflows/analyzer-ci.yml@main
    with:
      siblings: "capybari-fixtures capybari-analyzer-dependencies"
