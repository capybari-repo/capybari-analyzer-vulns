# Rules

Put rule files, signatures and patterns for this capability here (YAML or JSON), so the community can review and contribute to them without touching Go code. The example capability keeps its single pattern in code.
